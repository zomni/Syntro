{
  "name": "syntro",
  "version": "1.0.0",
  "description": "White-label template for indoor mapping, asset inventory and network telemetry",
  "private": true,
  "scripts": {
    "test": "jest",
    "build": "node create_dist.js && webpack",
    "dev": "docker compose -f ../docker-compose.yml up -d --build && echo \"\n************************************************\n* Started development stack on localhost:8080 *\n************************************************\n\"",
    "stop-dev": "docker compose -f ../docker-compose.yml down && echo \"\n****************************\n* Ended development stack  *\n****************************\n\""
  },
  "author": "Syntro contributors",
  "license": "MIT",
  "devDependencies": {
    "webpack": "^5.73.0",
    "webpack-cli": "^4.10.0",
    "jest": "^29.7.0",
    "babel-jest": "^29.7.0",
    "@babel/core": "^7.24.0",
    "@babel/preset-env": "^7.24.0"
  }
}
